---
description: Review this session for recurring pitfalls and route them into the intent layer
---

Review **this session** for pitfalls worth keeping, then route each one.

Use the `harvest-pitfalls` skill for the bar and the routing table. Do not re-read the transcript
from disk — you lived the session, and what you assumed is the part that matters and the part a
transcript can't recover.

Work from what actually happened:

- Where did you assume something the source didn't say, and find out you were wrong?
- What did you try twice because the first fix addressed the wrong cause?
- What did $USER have to correct more than once?
- What failed in a way specific to this repo's layout rather than to the task?

For each candidate, state it as *what you assumed → what was actually true*, then route it with the
skill's table. Land whatever survives in the pending commit.

**Most sessions yield nothing durable.** "Nothing worth keeping — the friction was ordinary
iteration" is a complete answer. Report it in one line rather than padding the list.

$ARGUMENTS
