---
description: Establish an intent layer on a repo that has none, by interviewing the subject-matter expert
allowed-tools: Bash(git ls-files:*), Bash(git log:*), Bash(git shortlog:*), Bash(git diff:*), Bash(git rev-parse:*), Bash(git check-ignore:*), Bash(wc:*), Read, Grep, Glob, Task, AskUserQuestion, Write, Edit
---

Build this repo's intent layer by interview. $USER is the subject-matter expert; you are the one who
reads the code and drafts.

Use the `capturing-intent-layer` skill for the campaign — survey, destination, chunk map, interview,
roll-up, compress, close. Use the `intent-layer` skill for where nodes belong and what earns a line;
capture does not get its own bar.

Start at Phase 0. Stop for approval at the chunk map — it is the checkpoint that cannot be fixed
later without re-interviewing.

If $ARGUMENTS names a path, restrict the campaign to it.

$ARGUMENTS
