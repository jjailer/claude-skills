{
  "description": "At git commit: flag intent-layer nodes whose code changed without them, and sessions showing signs of a recurring pitfall",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/intent_layer_check.py\""
          }
        ]
      }
    ]
  }
}
